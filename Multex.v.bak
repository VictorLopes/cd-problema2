module Multex (
    input A,
	 input B,
	 speed[2:0]
);
	output  [2:0] speed;
	
	wire NA, NB;
	
	not(NA, A);
	not(NB, B);
	
	and(speed[0], A, NB);
	and(speed[1], NA, B);
	and(speed[2], A, B);
	
endmodule
